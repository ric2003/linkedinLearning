name: Generate Podcast feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout Repo
        uses: actions/checkout@v3
      - name: run feed generator
        uses: ric2003/podcast-generator@main
      
      
    
